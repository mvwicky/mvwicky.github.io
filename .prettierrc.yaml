tabWidth: 2
arrowParens: always
overrides:
  - files: "*.djt"
    options:
      parser: "html"
      requirePragma: true
      printWidth: 120
  - files: "*.html"
    options:
      requirePragma: true
      printWidth: 120
