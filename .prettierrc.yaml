tabWidth: 2
arrowParens: always
overrides:
  - files: "*.djt"
    options:
      parser: "html"
      requirePragma: true
      printWidth: 120
  - files: ["*.html", "*.md"]
    options:
      requirePragma: true
      printWidth: 120
